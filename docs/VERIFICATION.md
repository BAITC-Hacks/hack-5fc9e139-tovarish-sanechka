# Проверка поставки

## Последняя проверка приложения

23 сентября 2026:

- Все 43 пункта сопоставлены с реализацией и проверками в
  [frontend/UX_FIXES.md](frontend/UX_FIXES.md).
- `.venv/bin/python -m pytest -q`: **34 passed**, 14,32 с.
- `npm --prefix web run build`: TypeScript и production build успешны.
  JS 710,24 kB, 222,10 kB gzip. Предупреждение Vite о бандле >500 kB сохраняется.
- `APP_URL=http://127.0.0.1:8080 CHROMIUM_PATH=/usr/sbin/chromium npm --prefix web run test:e2e`:
  **20 passed**, 29,9 с на финальном контейнере.
- Проверены поиск и его ошибки, сохранение старых результатов, два независимых
  списка, все кандидаты консолидации, пагинация, выгрузка полного отбора,
  истории Back/Forward, reload и shared URL, локальные заметки и отказ storage,
  экранирование CSV, реальные пороги, оба маршрута общего поиска и контрольные
  случаи дат. Стили canvas проверены на реальные ширины рёбер, читаемый размер
  подписей и красный пунктир конфликтного ребра.
- Скриншоты desktop/mobile просмотрены. На 1440×900 рабочая область начинается
  на 191 px, таблица — на 430 px. На 390×844 первым открыт список, начало на
  393 px. Переполнения страницы нет на 320/390/768/1024/1440 px. Проверены фокус
  после выбора, последовательность Tab, высота кнопок и отсутствие вложенной
  вертикальной прокрутки на телефоне.
- `docker compose up -d --build --wait --wait-timeout 180`: успешно,
  основной сервис на 127.0.0.1:8080 — healthy. Слои зависимостей из кэша.
- `docker compose exec -T app python healthcheck.py`: успешно; проверены
  хеши UI, analysis.json и трёх CSV текущего запуска.
- `git diff --check`: успешно. Новые зависимости не добавлены, Python-расчёт,
  config и Parquet не изменены.

Решения сохраняются только в браузере. Ссылка передаёт рабочий контекст;
список проверки и заметки передаются через CSV. Это ограничение явно указано
в интерфейсе. Автоматической синхронизации решений между устройствами нет.

## История и воспроизведение

Предыдущие замеры и сценарии отказа сохранены в
[истории проверок](archive/VERIFICATION_HISTORY.md). Команды текущего запуска
и тестирования — в [README](../README.md#4-установка-запуск-и-проверка).

Редактура документации сама по себе не означает нового прогона прикладных
тестов: результаты выше относятся к проверенной реализации приложения.
