# Проверка поставки

Дата: 23 сентября 2026. Окружение: Linux amd64, Docker Engine 29.8.0,
Compose 5.5.1 (совместимый CLI Compose v2), Chromium через Playwright 1.63.0.
Python 3.12.13, Node.js 24.20.0. Проверки не доказывают работу на всех ОС/архитектурах.

## Аналитика и сборка

- `uv sync --locked`: зависимости согласованы с lock-файлом.
- `python -m pytest -q`: 34 проверки входной валидации, полного графа,
  самопереводов, повторов, дат, seed, обрыва depth=4, правил, путей от seed,
  внешних потоков кластеров, устойчивости сети, повторяемости и блокировок.
- `python -m compileall -q run.py healthcheck.py case/starter`: успешно.
- `npm --prefix web run build`: TypeScript и production build успешно. Vite
  предупреждает о JS-бандле 679,1 kB (213,4 kB gzip); Cytoscape включён
  локально, CDN нет.
- Два запуска дают побайтово одинаковые три CSV и analysis.json.
- Локальный полный расчёт: 3,335 с; в контейнере чистой копии: 4,468 с;
  в основном контейнере: 4,591 с. Время от чтения Parquet до записи всех
  результатов, без установки зависимостей и сборки UI; предел ТЗ — 5 минут.
- Роли, порядок приоритетов и номера 88 кластеров совпадают с результатом
  до добавления путей и сценариев устойчивости.

## Чистая копия и контейнер

Файлы репозитория скопированы в новый `/tmp/hackalem-clean-q_du6fjp`;
локальные `.venv`, `node_modules`, `dist`, `out` и `.git` отсутствовали.
UI и Python-зависимости устанавливались только внутри build-стадий.
`docker compose config --quiet` и затем
`docker compose up -d --build --wait --wait-timeout 360` завершились успешно,
сервис app — healthy. Чтобы не занимать уже работающий 8080, только host-порт
в копии `compose.yaml` заменён на 8082; контейнерный порт и Dockerfile не
менялись. Основной репозиторий проверен той же командой на 127.0.0.1:8080;
его сервис также healthy.

Первый пробный образ выявил закрытые права каталогов из исходной копии;
Dockerfile исправлен (`a+rX`), итоговый образ проверен от uid 10001.
Исходные Parquet репозитория не менялись.

Ранее на выделенном тестовом проекте проверены механизмы отказа и восстановления;
после расширения аналитики эти сценарии повторно не запускались:

1. Restart → новый run_id, идентичные хеши результатов, healthy.
2. Второй `python run.py` в обслуживаемый каталог → явный отказ блокировки.
3. Дописан байт в CSV внутри контейнера → healthcheck завершился с ошибкой.
4. Restart → результаты восстановлены пересчётом, healthy.
5. Удалён nodes.parquet только внутри тестового контейнера, затем restart →
   exit code 1; старые HTTP-результаты недоступны.
6. `up --force-recreate --wait` → данные из образа восстановлены, healthy.
7. Расчёт с `docker run --network none` → успешно без внешних подключений.
8. SIGTERM при restart корректно завершает HTTP-сервер.

Healthcheck сверяет по HTTP index, assets, analysis.json и три CSV с хешами
полностью проверенного текущего запуска; дополнительно проверяет схемы и
число строк. В текущей сборке `docker compose exec -T app python healthcheck.py`
завершился успешно. Хеши контейнерных CSV из чистой копии и основного сервиса
совпали с приложенными файлами в `submission/`:

| Файл | SHA-256 |
|---|---|
| `nodes_roles.csv` | `8fc4e386bf11cb5fb95253d49a2adfb80dbccd16bda1a3cdf44d2be8327e7f25` |
| `clusters.csv` | `45e912d98a215de9bc9fbce3445e81bc194d830b4c580c70496de43ca5010867` |
| `top_nodes.csv` | `0f74b8664db292aa3d989f5b01dff98e4259a8b69575c9f1e5bc40156dcba183` |

## Браузер

Все 5 Playwright-сценариев прошли против контейнера чистой копии:
поиск реального gid, карточка и выбор строки, фильтры и их сброс, неизвестный
gid, изолят, полный список связей, скачивание CSV, loading/error/retry,
повреждённый JSON, расширение большого кластера, выбор с клавиатуры и
все три скачивания CSV. Новый сценарий проверил путь из четырёх рёбер,
следующий запрос, таблицу устойчивости и мобильный экран. Не было pageerror,
внешних запросов и горизонтального переполнения страницы. Скриншоты пути
desktop/mobile и SVG-слайда просмотрены; SVG прошёл проверку XML.
Браузерные контексты закрываются после тестов.

Проверки устойчивости аналитики и её ограничения — в ANALYTICS.md.
Скоры эвристические; никакой accuracy или подтверждённой виновности
по этим проверкам не заявляется.
