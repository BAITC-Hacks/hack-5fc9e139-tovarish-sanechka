# Правила анализа, версия 1.0

Все роли — гипотезы для проверки. `role_score` — эвристическая выраженность
правила с поправкой на конкуренцию; это не вероятность нарушения.
Данных для измерения accuracy нет. Числа в config.toml не зависят от gid.

## Признаки и пороги

Основа порогов — распределения в [DATA_PROFILE.md](DATA_PROFILE.md). Число плательщиков 3 и
получателей 5 соответствует p95 по всему графу; минимальный оборот 164 528 ₸
соответствует p75 входящего объёма. Betweenness 0,0001553 — округлённый p95,
охват 9 seed — p90, число внешних соседних кластеров 2 — p95.
Доля выхода с недавним входом 0,69 — округлённый p90 по всем узлам.

Остальные границы — интерпретируемые ограничения, а не обученные пороги:
концентрация ≤0,8 не даёт одному контрагенту полностью объяснять веер/сбор;
охват минимум 2 означает несколько ветвей seed. Транзит требует близких
объёмов (0,7–1,4; около наблюдаемой медианы 1,065 у не-seed с входом и выходом).
Удержание требует выхода ≤10% входа — около p10=0,105 в той же подвыборке.
Эти границы нуждаются в ручной проверке и не являются банковскими нормативами.

| Роль | Все обязательные условия |
|---|---|
| consolidator | ≥3 плательщиков, узел достижим от ≥2 разных seed, максимальная доля одной входящей пары ≤0,8 |
| distributor | ≥5 получателей, максимальная доля одного исходящего контрагента ≤0,8 |
| transit | не seed, есть вход и выход к другим узлам, суммарные вход и выход каждый ≥164 528 ₸, out/in от 0,7 до 1,4, доля близости дат ≥0,69 |
| terminal | не seed, не обрыв depth=4 без внешних получателей, есть другой плательщик, суммарный вход ≥164 528 ₸, out/in ≤0,1 |
| coordinator | узел достижим от ≥9 разных seed, ≥2 внешних соседних кластеров, направленная невзвешенная betweenness ≥0,0001553 |
| peripheral | ни одно из перечисленных правил не сработало |

Для допустимых правил вычисляется score:

- consolidator: `0,6 + 0,2 min(in_deg / 6, 1) + 0,2 (1 − in_concentration)`;
- distributor: `0,6 + 0,2 min(out_deg / 10, 1) + 0,2 (1 − out_concentration)`;
- transit: `0,6 + 0,2 min(ratio, 1/ratio) + 0,2 timing_share`;
- terminal: `0,6 + 0,2 (1 − ratio) + 0,2 min(in_kzt / 329056, 1)`;
- coordinator: `0,6 + 0,2 min(betweenness / 0,0003106, 1) + 0,2 min(neighbor_cluster_count / 4, 1)`.

Формулы выше приведены для базового `config.toml`. Знаменатели 6, 10,
329056, 0,0003106 и 4 равны удвоенным соответствующим порогам config;
при изменении этих порогов меняются и знаменатели.
Берётся наибольший допустимый score. При равенстве — порядок `role_order`
в config: coordinator, consolidator, distributor, transit, terminal, peripheral.
Итоговый role_score умножается на `(1 − 0,2 × лучший альтернативный score)`.
У peripheral фиксированное 0,25: это слабая определённость при отсутствии
оснований, а не уверенность в безвредности. Все исходные скоры доступны в
`role_scores`, факты — в признаках и evidence. Значения недопустимых ролей равны 0.

## Приоритет

Приоритет не зависит от выбора одной роли и не равен role_score:

`0,35 N(seed_reach) + 0,40 [N(betweenness)+N(in_deg+out_deg)]/2 + 0,25 N(in_kzt+out_kzt)`.

`N(x)=log(1+x)/max(log(1+x))` по всем узлам текущего запуска. При нулевом
максимуме N=0; у постоянного положительного признака N=1 для всех — он не
различает узлы. Веса выражают предпочтение структурного охвата (75%) перед
объёмом (25%); они не оптимизированы на отсутствующей разметке.
Вклады сохранены отдельно. Равенства приоритета разрешаются по числовому gid.
Топ содержит все узлы с последовательными rank от 1, что превышает минимум 20.

Выборка «Новые: основная или альтернативная консолидация» оставляет узлы с `is_seed=false` и
`role_scores.consolidator>0`: правило консолидации должно сработать, даже
если основная роль другая. На исходных данных это 158 кандидатов: 65 с
основной ролью consolidator и 93 с альтернативной гипотезой. Сохраняются
общий приоритет и глобальный rank. Смена выборки сохраняет фильтры основной
роли и кластера, а также сортировку; открывается первая страница. Поиск gid
продолжает охватывать все узлы. Выборка не меняет модель и обязательные CSV;
отфильтрованный список можно выгрузить отдельно в браузере.

## Граф, даты и кластеры

Все 2 248 узлов входят в граф. PageRank взвешен по sum_kzt; ошибка сходимости
прерывает расчёт. Betweenness направленная, без весов длины. Достижимость от
каждого seed считается отдельно; узел не считается достижимым от самого себя.
Самопереводы входят в оборот, но не в число разных контрагентов и не во
временной признак. Неопределённые отношения в JSON равны null.
`reachable_seed_count` означает число исходных клиентов, **от которых
достижим проверяемый узел**, а не число seed, достижимых из него.
Суммы, число операций, отношение out/in и концентрации включают самопереводы;
в исходном датасете самопереводов нет. `truncated_by_depth` истинен только
при `depth=4` и отсутствии получателей, отличных от самого узла.

Для каждой исходящей даты суммируется объём, если есть вход в тот же день
или за два предшествующих дня; каждый исходящий объём учитывается один раз.
Это близость дат, не сопоставление денег. Внутридневной порядок неизвестен.
Повторы транзакций сохранены.

Louvain работает на ненаправленной проекции с суммой встречных потоков,
без петель. Seed=42, resolution=1; порядок добавления узлов/рёбер фиксирован.
Изоляты получают отдельные кластеры. Номера кластеров идут по минимальному
числовому gid. Выбор resolution=1 — исходный масштаб модульности, не подбор
под число сообществ из примечания организаторов. Получено 88 кластеров.
`top_gids` — до 5 лидеров по приоритету, в CSV JSON-массив десятичных строк.
Внутренний оборот включает петли и каждое направленное ребро один раз.

## Наблюдаемый путь и гипотеза кластера

Для каждого узла выбирается кратчайший направленный путь от исходного узла.
При равной длине сначала выбирается меньший числовой gid исходного узла,
затем лексикографически меньшая последовательность gid. Для самого seed путь
нулевой длины не рассматривается: если другой seed до него не достигает,
показывается один исходный узел без рёбер. Для недостижимого не-seed путь
пуст. Путь сохранён в `seed_path_gids` (строки в JSON и в JSON-массиве CSV);
каждый его шаг проверяется против направленных рёбер. Суммы на рёбрах
агрегированы за месяц и не доказывают прохождение одних средств по цепочке.

`next_check` — запрос данных для закрытия главного пробела: выход за четвёртым
коленом, полные входящие seed, хронология для transit либо соседние периоды.
Это рекомендация аналитику, не новый признак роли.

Интерактивный поиск общих узлов от 2–5 seed использует направленные рёбра
готового `analysis.json`: для каждого введённого seed выполняется обход в
ширину, затем берётся пересечение достижимых не-seed узлов. Результаты идут
по уже рассчитанному приоритету. Отдельный кратчайший путь от каждого seed
выбирается детерминированно по возрастанию gid при равной длине. Поиск не
пересчитывает роли или приоритеты; общая достижимость не означает, что
наблюдался один и тот же денежный поток.

### Проверка дат показанного пути

Для каждого направленного ребра JSON версии 3 содержит `dates`: уникальные
даты исходных операций по этой паре, отсортированные по возрастанию.
Повторы операций не удаляются из исходных данных; сокращается только список дат.
Сумма и число операций на ребре по-прежнему относятся ко всему месяцу.

Одна функция интерфейса проверяет пути от seed и пути поиска общих узлов.
Для фиксированной последовательности рёбер выбирается самая ранняя допустимая
дата каждого следующего шага. Такой выбор оставляет максимум возможностей
для продолжения. Первый проход требует строго возрастающих дат, второй
(если первый неуспешен) допускает одинаковые даты соседних шагов.
Результаты: согласованная последовательность; неизвестный порядок внутри
дня; отсутствие даже неубывающей последовательности. В последнем случае
показаны первый конфликтующий шаг второго прохода и доступные даты операций.
Для одного ребра показывается дата, для пути без рёбер — сообщение об отсутствии пути.

Это проверка выбранного структурного пути, а не всех возможных маршрутов.
Его несогласованность не исключает альтернативного маршрута; согласованность
не доказывает происхождение средств, совпадение сумм или их сохранение.
Проверка не влияет на роли, приоритеты, кластеры или обязательные CSV.

### Альтернативный путь по датам

При конфликте или неизвестном внутридневном порядке приложение ищет путь для
тех же концов. Поиск в ширину выполняется по состояниям `(gid, дата прихода)`,
сначала со строго возрастающими датами, затем — с неубывающими. Первое найденное
решение в выбранном режиме имеет минимум рёбер. Поэтому строгий путь может
быть длиннее пути с совпадающими датами. Пустой путь к самому себе имеет ноль рёбер.

Соседи упорядочены по числовому gid без преобразования в JavaScript number,
даты — по возрастанию. Каждое состояние посещается один раз; циклы не вызывают
бесконечный поиск. Ограничения в четыре ребра нет: четыре колена относятся
к сбору данных, а не к длине всех маршрутов. Для N узлов, E направленных пар
и D наблюдаемых дней консервативная верхняя граница реализации —
O(D²·E+D·N) по времени и O(D·N+E) по памяти: для каждого состояния
перебираются даты исходящих рёбер. На данном наборе D≤31.

Результат: `ordered`, `same_day` или `not_found`, последовательность gid и даты.
`not_found` означает отсутствие согласованного пути только в наблюдаемом графе.
Путь и исходный вариант сохраняются в URL; возврат и browser history восстанавливают их.
Фильтры общего поиска по-прежнему относятся к выбранным структурным кратчайшим путям.

Регрессионный пример: от `100000003016635100` к `100000000490383100` кратчайший
путь через `100000002938501100` имеет даты 8 → 6 июля. Альтернатива через
`100000005287097100` согласована: 11 → 19 июля. Эти gid используются только
в демонстрации и тестах, не в алгоритме.

### Операции и досье

Каждое ребро содержит `operations`: `{index, date, sum_kzt}`. `index` —
нулевой порядковый номер строки исходного `transactions.parquet`; в досье
показывается номер `index+1`. Одинаковые операции сохраняются отдельно.
Проверяются уникальность и полнота индексов, соответствие числу операций,
месячной сумме и набору дат ребра. `gid` в JSON остаются строками.

Досье одного узла включает пути от источников активного общего поиска либо
представительный путь от seed, а также альтернативы при конфликте дат.
Для выбранной даты перечисляются все операции ребра в этот день. Если
согласованная дата не выбрана, перечисляются все операции ребра с явной подписью.
Ни одна из них автоматически не объявляется источником следующего перевода.
Месячные суммы отделены от сумм операций. Документ содержит ограничения,
сценарии порогов, локальное решение и заметку аналитика; формируется без LLM.

Гипотеза кластера описывает внешний вход и выход относительно других
сообществ, число seed и узлов с непериферийной ролью. При большем внешнем
выходе используется осторожное название «передающий фрагмент», при большем
входе — «получающий». Изоляты описываются отдельно. Это характеристика
наблюдаемой выборки, не установленное назначение группы.

## Устойчивость наблюдаемой сети

Берётся крупнейшая исходная слабосвязная компонента (1 877 узлов). Для
N=5, 10, 20 из неё исключаются первые N по приоритету, затем считаются
размер крупнейшего оставшегося фрагмента и число фрагментов. Контроль —
первые N узлов этой же компоненты по направленной суммарной степени и медиана
100 случайных исключений N узлов из неё. Случайный генератор фиксирован
значением `random_seed` из config. На исходных данных:

| Исключено | Крупнейший фрагмент: приоритет | Фрагментов | По степени | Случайно, медиана |
|---|---:|---:|---:|---:|
| 5 | 1 624 | 152 | 1 556 | 1 869,5 |
| 10 | 1 414 | 278 | 1 371 | 1 860 |
| 20 | 1 155 | 444 | 1 099 | 1 840 |

Это модель связности ограниченного наблюдаемого графа, не прогноз эффекта
блокирования счетов и не доказательство преступной роли топ‑узлов.

## Чувствительность правил и текущий результат

На исходных данных: peripheral 1751, terminal 282, distributor 119,
consolidator 72, transit 19, coordinator 5. Время актуального полного расчёта
Parquet → четыре файла приведено в [VERIFICATION.md](VERIFICATION.md).
Повторный расчёт даёт побайтово одинаковые CSV и JSON.

JSON v3 содержит три сценария (`sensitivity`: `lower`, `base`, `higher`) и
`role_sensitivity` каждого узла. Совместно изменяются ровно шесть порогов:
`min_payers`, `min_recipients`, `min_volume`, `coordinator_seed_reach`,
`min_neighbor_clusters`, `min_betweenness`. Множители: 0,9 / 1 / 1,1;
остальные пороги, веса, признаки, кластеры и порядок разрешения равенств прежние.
Сравнения используют значения без округления: ≥3,3 плательщика фактически
требует ≥4. Интерфейс показывает и порог, и необходимое целое число.

Каждый сценарий вызывает то же правило назначения роли. Причины показывают
пересечённые пороги; если меняется победитель среди допустимых ролей,
показываются исходные баллы конкурентов. Балл роли учитывает конкуренцию.
Сценарии не подменяют базовую роль и не меняют рейтинг или обязательные CSV.
Результат на исходных данных:

| Изменение | Смена роли | Доля |
|---|---:|---:|
| −10% | 27 / 2248 | 1,20% |
| +10% | 83 / 2248 | 3,69% |

При +10% coordinator исчезает: все пять кандидатов чувствительны к границам.
Это существенная неопределённость роли, а не свидетельство устойчивой
организаторской функции. Приоритет при таком изменении не меняется,
поскольку использует непрерывные признаки. Это локальная проверка
чувствительности, не доказательство качества модели.

Ограничения выборки (seed, depth=4, июль, внутрибанковские операции и порог
5000 ₸) сохраняются в meta, data_warnings и кратких объяснениях. Термин
terminal означает наблюдаемое удержание внутри выборки. Ни одна роль
не подтверждает преступление. Следующий шаг аналитика — запрос полных
входящих seed, продолжения depth=4 и операций за соседние периоды.
